<Window x:Class="OrganicVectoriser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:OrganicVectoriser.ViewModels"
        mc:Ignorable="d"
        Title="Organic Segmentation Vectoriser" Height="720" Width="1200">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Left: scrollable parameters -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="4">
                <TextBlock Text="Segmentation Parameters" FontWeight="Bold" FontSize="14" />
                <Separator />

                <TextBlock Text="Noise Scale" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="10" Maximum="200" TickFrequency="5" Width="220" Value="{Binding Parameters.NoiseScale, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.NoiseScale, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Blur Sigma" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0.1" Maximum="10" TickFrequency="0.1" Width="220" Value="{Binding Parameters.BlurSigma, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.BlurSigma, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Compactness" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0.0001" Maximum="1" TickFrequency="0.001" Width="220" Value="{Binding Parameters.Compactness, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.Compactness, StringFormat={}{0:0.####}}" />
                </StackPanel>

                <TextBlock Text="Max Colors" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="2" Maximum="32" TickFrequency="1" IsSnapToTickEnabled="True" Width="220" Value="{Binding Parameters.MaxColors, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.MaxColors, StringFormat={}{0:0}}" />
                </StackPanel>

                <TextBlock Text="Segmentation Multiplier" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0.1" Maximum="10" TickFrequency="0.1" Width="220" Value="{Binding Parameters.SegmentationMultiplier, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.SegmentationMultiplier, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <Separator Margin="0 8 0 8" />
                <TextBlock Text="Bridging Parameters" FontWeight="Bold" />

                <TextBlock Text="Bridge Distance" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="100" TickFrequency="1" Width="220" Value="{Binding Parameters.BridgeDistance, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.BridgeDistance, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Color Tolerance" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="100" TickFrequency="1" Width="220" Value="{Binding Parameters.ColorTolerance, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.ColorTolerance, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Proximity Threshold" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="200" TickFrequency="1" Width="220" Value="{Binding Parameters.ProximityThreshold, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.ProximityThreshold, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Falloff Radius" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="1" Maximum="30" TickFrequency="1" IsSnapToTickEnabled="True" Width="220" Value="{Binding Parameters.FalloffRadius, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.FalloffRadius, StringFormat={}{0:0}}" />
                </StackPanel>

                <TextBlock Text="Max Curvature (degrees)" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="1" Maximum="360" TickFrequency="1" Width="220" Value="{Binding Parameters.MaxCurvatureDegrees, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.MaxCurvatureDegrees, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <Separator Margin="0 8 0 8" />
                <TextBlock Text="Smoothing &amp; Inflation" FontWeight="Bold" />

                <TextBlock Text="Smooth Iterations" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="1" Maximum="20" TickFrequency="1" IsSnapToTickEnabled="True" Width="220" Value="{Binding Parameters.SmoothIterations, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.SmoothIterations, StringFormat={}{0:0}}" />
                </StackPanel>

                <TextBlock Text="Smooth Alpha" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0.01" Maximum="1" TickFrequency="0.01" Width="220" Value="{Binding Parameters.SmoothAlpha, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.SmoothAlpha, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Blob Inflation Amount" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="50" TickFrequency="0.1" Width="220" Value="{Binding Parameters.BlobInflationAmount, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.BlobInflationAmount, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Far Point Inflation Factor" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="5" TickFrequency="0.01" Width="220" Value="{Binding Parameters.FarPointInflationFactor, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.FarPointInflationFactor, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <CheckBox Content="Inflation Proportional to Stacking" IsChecked="{Binding Parameters.InflationProportionalToStacking, Mode=TwoWay}" />

                <Separator Margin="0 8 0 8" />
                <TextBlock Text="Droplet Parameters (Painterly)" FontWeight="Bold" />

                <TextBlock Text="Droplet Density" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="10" TickFrequency="1" IsSnapToTickEnabled="True" Width="220" Value="{Binding Parameters.DropletDensity, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletDensity, StringFormat={}{0:0}}" />
                </StackPanel>

                <TextBlock Text="Droplet Min Distance" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="100" TickFrequency="1" Width="220" Value="{Binding Parameters.DropletMinDistance, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletMinDistance, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Droplet Max Distance" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="200" TickFrequency="1" Width="220" Value="{Binding Parameters.DropletMaxDistance, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletMaxDistance, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Droplet Size Mean" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="1" Maximum="50" TickFrequency="0.1" Width="220" Value="{Binding Parameters.DropletSizeMean, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletSizeMean, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Droplet Size Std" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="20" TickFrequency="0.1" Width="220" Value="{Binding Parameters.DropletSizeStd, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletSizeStd, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Droplet Spread (degrees)" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="90" TickFrequency="1" Width="220" Value="{Binding Parameters.DropletSpreadDegrees, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletSpreadDegrees, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Droplet Global Rotation (degrees)" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="-180" Maximum="180" TickFrequency="1" Width="220" Value="{Binding Parameters.DropletGlobalRotation, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletGlobalRotation, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <CheckBox Content="Use SVG Primitives (Ellipse/Rect)" IsChecked="{Binding Parameters.PainterlyUseSvgEllipses, Mode=TwoWay}" />
                <CheckBox Content="Rect Horizontal Mode" IsChecked="{Binding Parameters.PainterlyRectHorizontal, Mode=TwoWay}" />

                <Separator Margin="0 8 0 8" />
                <TextBlock Text="Organic Droplet Parameters" FontWeight="Bold" />

                <TextBlock Text="Organic Min Brightness (0-255)" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="255" TickFrequency="1" Width="220" Value="{Binding Parameters.DropletOrganicMinBrightness, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletOrganicMinBrightness, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Organic Density" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="20" TickFrequency="1" IsSnapToTickEnabled="True" Width="220" Value="{Binding Parameters.DropletOrganicDensity, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletOrganicDensity, StringFormat={}{0:0}}" />
                </StackPanel>

                <TextBlock Text="Organic Strength" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="5" TickFrequency="0.01" Width="220" Value="{Binding Parameters.DropletOrganicStrength, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletOrganicStrength, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Organic Jitter" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="5" TickFrequency="0.01" Width="220" Value="{Binding Parameters.DropletOrganicJitter, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletOrganicJitter, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Organic Elongation" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="-2" Maximum="5" TickFrequency="0.01" Width="220" Value="{Binding Parameters.DropletOrganicElongation, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletOrganicElongation, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Organic Percent Per Blob (%)" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="100" TickFrequency="0.1" Width="220" Value="{Binding Parameters.DropletOrganicPercentPerBlob, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.DropletOrganicPercentPerBlob, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <Separator Margin="0 8 0 8" />
                <TextBlock Text="Other Settings" FontWeight="Bold" />

                <TextBlock Text="Simplify Tolerance (pixels)" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Slider Minimum="0" Maximum="5" TickFrequency="0.01" Width="220" Value="{Binding Parameters.SimplifyTolerance, Mode=TwoWay}" />
                    <TextBlock Width="70" Margin="8 0 0 0" Text="{Binding Parameters.SimplifyTolerance, StringFormat={}{0:0.##}}" />
                </StackPanel>

                <TextBlock Text="Stacking Order" />
                <ComboBox SelectedValue="{Binding Parameters.StackingOrder, Mode=TwoWay}">
                    <ComboBoxItem Content="area" />
                    <ComboBoxItem Content="area_reverse" />
                    <ComboBoxItem Content="brightness" />
                    <ComboBoxItem Content="brightness_reverse" />
                    <ComboBoxItem Content="position_x" />
                    <ComboBoxItem Content="position_x_reverse" />
                    <ComboBoxItem Content="position_y" />
                    <ComboBoxItem Content="position_y_reverse" />
                    <ComboBoxItem Content="position_centre" />
                    <ComboBoxItem Content="position_centre_reverse" />
                </ComboBox>

                <Separator Margin="0 8 0 8" />
                <TextBlock Text="Modifiers" FontWeight="Bold" />

                <CheckBox Content="Color Quantization" IsChecked="{Binding Modifiers.ColorQuantization, Mode=TwoWay}" />
                <CheckBox Content="Bridging" IsChecked="{Binding Modifiers.Bridging, Mode=TwoWay}" />
                <CheckBox Content="Smoothing" IsChecked="{Binding Modifiers.Smoothing, Mode=TwoWay}" />
                <CheckBox Content="Inflation" IsChecked="{Binding Modifiers.Inflation, Mode=TwoWay}" />
                <CheckBox Content="Enable Vector Preview" IsChecked="{Binding Modifiers.EnableVectorPreview, Mode=TwoWay}" />

                <Separator Margin="0 8 0 8" />
                <TextBlock Text="SVG Output Path" />
                <TextBox Text="{Binding SvgOutputPath, Mode=TwoWay}" />
            </StackPanel>
        </ScrollViewer>

        <!-- Middle: actions/status -->
        <StackPanel Grid.Column="1" Margin="8" VerticalAlignment="Stretch">
            <Button Content="Load Image" Command="{Binding LoadImageCommand}" Margin="0 0 0 8" />
            <Button Content="Preview" Command="{Binding PreviewCommand}" Margin="0 0 0 8" />
            <Button Content="Run Vectoriser" Command="{Binding RunCommand}" Margin="0 0 0 8" />
            <Button Content="Run Diagnostics" Command="{Binding DiagnosticsCommand}" Margin="0 0 0 8" Background="#FFF0E68C" ToolTip="Debug pipeline stages individually" />

            <ProgressBar Minimum="0" Maximum="100" Height="18" Value="{Binding Progress}" />
            <TextBlock Text="{Binding Status}" TextWrapping="Wrap" Margin="0 6 0 0" />

            <Separator Margin="0 12 0 12" />
            <TextBlock Text="Presets (TBD)" FontWeight="Bold" />
            <StackPanel Orientation="Horizontal" Margin="0 4 0 0">
                <TextBox Width="140" Text="{Binding CurrentPresetName, Mode=TwoWay}" />
                <Button Content="Save" Command="{Binding SavePresetCommand}" Margin="6 0 0 0" />
            </StackPanel>
            <ComboBox ItemsSource="{Binding AvailablePresets}" SelectedItem="{Binding SelectedPreset}" />
            <Button Content="Load" Command="{Binding LoadPresetCommand}" Margin="0 4 0 0" />
            <Button Content="Delete" Command="{Binding DeletePresetCommand}" Margin="0 4 0 0" />

            <Separator Margin="0 12 0 12" />
            <StackPanel Orientation="Horizontal">
                <Button Content="Undo" Command="{Binding UndoCommand}" Margin="0 0 6 0" />
                <Button Content="Redo" Command="{Binding RedoCommand}" />
            </StackPanel>
        </StackPanel>

        <!-- Right: preview -->
        <Border Grid.Column="2" Margin="8" BorderBrush="Gray" BorderThickness="1" Background="#111">
            <Grid>
                <Viewbox Stretch="Uniform">
                    <Image Source="{Binding PreviewImage}" Width="800" Height="600" />
                </Viewbox>
                <TextBlock Text="Preview pending" Foreground="Gray" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding IsPreviewAvailable, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=false}" />
            </Grid>
        </Border>
    </Grid>
</Window>
